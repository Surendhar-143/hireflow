{
  "buildCommand": "cd ../.. && pnpm install && pnpm --filter @hireflow/schemas build && pnpm --filter @hireflow/web build",
  "outputDirectory": "dist",
  "installCommand": "npm install -g pnpm@11.2.2",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
