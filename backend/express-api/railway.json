{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "if [ -f ../../package.json ]; then cd ../..; fi && pnpm install && pnpm --filter @hireflow/schemas build && cd backend/express-api && npx prisma generate && pnpm build"
  },
  "deploy": {
    "startCommand": "if [ -d backend/express-api ]; then cd backend/express-api; fi && node dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/health"
  }
}
