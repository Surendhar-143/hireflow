{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && pnpm --filter @hireflow/schemas build && cd backend/express-api && npx prisma generate && pnpm build"
  },
  "deploy": {
    "startCommand": "node backend/express-api/dist/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3,
    "healthcheckPath": "/api/v1/health"
  }
}
